{
    "name": "monorepo",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "dev": "npm run dev --prefix apps/web",
        "build": "npm run build --prefix apps/web",
        "lint": "npm run lint --prefix apps/web"
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    },
    "workspaces": [
        "apps/*"
    ],
    "dependencies": {
        "html2canvas": "^1.4.1",
        "jspdf": "^4.2.1",
        "path": "^0.12.7"
    }
}
